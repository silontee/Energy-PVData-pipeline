apiVersion: 1

datasources:
  - name: PostgreSQL
    type: postgres
    access: proxy
    url: pv-db:5432
    database: ${DB_NAME}
    user: ${DB_USER}
    secureJsonData:
      password: ${DB_PASS}
    jsonData:
      sslmode: 'disable' 
      postgresVersion: 15
    editable: true